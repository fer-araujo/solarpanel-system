services:
  solar:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "8787:8787"
    volumes:
      # Persist CFE meter readings across restarts and rebuilds.
      - ./data:/app/data
